public enum MessageIDs
{
    BulletImpact,
    BulletImpactBlood,

    NoiseGenerated,

    AnyKeyPressed,

    ScopeIn,
    Unscope,
    
    ReloadStart,
    ReloadFinish,
    ReloadCancel,

    SprintStart,
    SprintEnd,

    LevelStart,
    LevelEnd,
    PlayerDeath,

    AnimalDeath,
    AnimalFlee,
    AnimalEnterBoundary,
    AnimalExitBoundary,

    ShowTargetTracker,

    BulletTimeStart,
    BulletTimeEnd,

    QuestComplete,
    QuestFailed,
    DayComplete,
}